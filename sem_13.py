"""solicitud de tados remotos en formato de json mediante libreria request
si no se dispone la libreria, desde consola ejecutar pip install request
para ver listado de librerias oficiales pypi.org
backup copia de seguridad local
conceptos de  peticiones URLs y argumentos parametros"""

import requests

def recupera_remoto():
    consulta= requests.get()


if (__name__=="__main__"):

